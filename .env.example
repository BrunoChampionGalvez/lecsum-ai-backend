# LecSum AI — Backend (.env.example)

# --- Application ---
PORT=3001
ENVIRONMENT=development
FRONTEND_URL=http://localhost:3000
NODE_OPTIONS=--max_old_space_size=4096

# --- JWT ---
JWT_SECRET=change-me-please

# --- Database (PostgreSQL) ---
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=lecsum

# --- AI Providers ---
# Gemini API key used by the AI service
GEMINI_API_KEY=your-gemini-api-key

# --- Vector DB (Pinecone) ---
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX_NAME=your-index-name
PINECONE_INDEX_HOST=https://your-index-host.svc.YOUR-REGION.pinecone.io

# --- File storage ---
# Local uploads directory (created if it doesn't exist)
UPLOADS_DIR=uploads

# Google Cloud Storage (if using GCS for uploads)
GCS_BUCKET_NAME=your-gcs-bucket
GCS_ACCESS_TOKEN=your-gcs-access-token
